spring:
  application:
    name: callme-service
  config:
    import: consul
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        healthCheckInterval: 5s
        healthCheckTimeout: 10s
        healthCheckCriticalTimeout: 15s
