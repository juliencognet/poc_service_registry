spring:
  application:
    name: caller-service-2-3
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        enabled: true
        instance-id: ${spring.application.name}:${random.int}
        heartbeat:
          enabled: true

    loadbalancer:
      cache:
        enabled: false