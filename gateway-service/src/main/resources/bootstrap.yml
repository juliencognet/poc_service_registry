spring:
  application:
    name: gateway-service
  cloud:
    consul:
      host: consul
      config:
        format: YAML
      discovery:
       # hostname: localhost
        register-health-check: false

    loadbalancer:
      cache:
        ttl: PT5S
      ribbon:
        enabled: false

server.port: 8080