spring:
  application:
    name: caller-service
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        enabled: false
        instance-id: ${spring.application.name}:${random.int}
