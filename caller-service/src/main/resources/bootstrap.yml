spring:
  application:
    name: caller-service
  cloud:
    consul:
      host: localhost
      discovery:
#        hostname: localhost
#        register-health-check: false
        instance-id: ${spring.application.name}:${random.int}
