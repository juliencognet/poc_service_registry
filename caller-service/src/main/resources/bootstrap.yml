spring:
  application:
    name: caller-service
  cloud:
    consul:
      host: consul
      port: 8500
      discovery:
        enabled: true
        instance-id: ${spring.application.name}:${random.int}

    loadbalancer:
      cache:
        enabled: false